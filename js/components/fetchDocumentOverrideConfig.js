// Configuration class for FetchDocumentOverrider
export class FetchDocumentOverrideConfig {
    constructor(url, target, template, mode) {
        this.url = url;
        this.target = target;
        this.template = template;
        this.mode = mode;
    }
}
